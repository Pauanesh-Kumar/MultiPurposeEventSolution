apiVersion: v1
kind: Service
metadata:
  name: app-api-svc
  labels:
    app: app-api
spec:
  type: LoadBalancer
  selector:
    app: app-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
